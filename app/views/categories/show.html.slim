h1 Show Category

h2= @category.name

= link_to "New Product", new_category_product_path(@category)

= nested_categories @category.subtree.arrange(:order => :created_at)

= render @category.products

= link_to 'Back', categories_path
