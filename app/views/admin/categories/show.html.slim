h1 Show Category

h2= @category.name

= link_to "New Product", new_admin_category_product_path(@category)

#admin_tree
  = nested_categories @category.subtree.arrange

= render @category.products

= link_to 'Back', admin_categories_path
